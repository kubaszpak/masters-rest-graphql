config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - flow:
      - get:
          url: "/"
      - get:
          url: "/slow"
      - post:
          url: "/data"
          json:
            name: "Alice"
            age: 30
